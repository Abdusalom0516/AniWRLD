class AnimeInfoFull {
  String title, trailerUrl, detailImg, synopsis, thumbnail;
  double score;
  int year;


  AnimeInfoFull(this.title, this.trailerUrl, this.detailImg, this.synopsis,
      this.thumbnail, this.score, this.year);
}
